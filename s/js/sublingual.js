(function(){
  var dave = {};
  
  // function to call markdown from admin every second and update the preview area with the response
  // preview button posts to markdown & is captured in case ajax call has failed
  
}());